import React from 'react';

// Simple test component to verify duplicate key handling
const DuplicateKeyTest: React.FC = () => {
  // This is just a test component to verify our fixes
  // In a real scenario, this would be removed after verification
  
  return (
    <div>
      <h1>Duplicate Key Test Component</h1>
      <p>This component is for testing duplicate key handling fixes.</p>
    </div>
  );
};

export default DuplicateKeyTest;